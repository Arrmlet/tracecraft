# tracecraft

The black box for AI agents — record every session, referee every task claim, all in a bucket you own.

Full documentation lives in the repository root: **[github.com/Arrmlet/tracecraft](https://github.com/Arrmlet/tracecraft#readme)**

> This file is replaced by the root README at release time (see `.github/workflows/release.yml`), so the PyPI page always renders the full docs.

```bash
pip install tracecraft-ai
```
